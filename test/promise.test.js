
const { promise } = require('../src/promise');

describe('A function that returns a promise', ()=>{
  test('it returns an array of objects', () => {
    
  });
});

// Look into = > https://www.npmjs.com/package/jest-fetch-mock